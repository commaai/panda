#pragma once

// Body v2 board detection
// Since body_v2 is a single board variant, no auto-detection needed
void detect_board_type(void) {
  // Board type set explicitly in main()
  // No GPIO detection required
}
